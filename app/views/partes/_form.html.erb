<%= form_with(model: parte, local: true) do |form| %>
  <% if parte.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parte.errors.count, "error") %> prohibited this parte from being saved:</h2>

      <ul>
      <% parte.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :parte_nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
