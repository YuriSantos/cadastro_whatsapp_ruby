<% provide(:current_page, 'Solicitar Alteração') %>

<div class="row">
  <div class="col-xs-12">
    <div class="box box-primary">





      <table class="table table-striped">
        <thead>
        <tr>
          <th>Nome</th>
          <th>informações</th>
          <th>usuário</th>
          <th>visualizado</th>
          <th colspan="3"></th>
        </tr>
        </tr>
        </thead>

        <tbody>


        <% @solicitacao_alts.each do |solicitacao_alt| %>
            <tr>
              <td><%= solicitacao_alt.cadastro_id %></td>
              <td><%= solicitacao_alt.informacoes %></td>
              <td><%= solicitacao_alt.usuario %></td>
              <td><%= solicitacao_alt.visualizado %></td>
              <td class="col-xs-1">
                <%= link_to solicitacao_alt do %>
                    <i class="fa fa-2x fa-eye"></i>
                <% end %>
              </td>
              <td class="col-xs-1">
                <%= link_to edit_solicitacao_alt_path(solicitacao_bloq) do %>
                    <i class="fa fa-2x fa-edit"></i>
                <% end %>
              </td>
              <td class="col-xs-1">
                <%= link_to solicitacao_alt, method: :delete, data: { confirm: 'Tem certeza que deseja excluir?!' } do %>
                    <i class="fa fa-2x fa-trash"></i>
                <% end %>
              </td>
            </tr>
        <% end %>
        </tbody>
      </table>

      <br>

      <div class="push-right">

      </div>

    </div>
  </div>
</div>