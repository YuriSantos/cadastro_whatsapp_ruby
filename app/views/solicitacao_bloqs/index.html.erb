<% provide(:current_page, 'Solicitar Bloqueio') %>

<div class="row">
  <div class="col-xs-12">
    <div class="box box-primary">





      <table class="table table-striped">
        <thead>
        <tr>
          <th>Nome</th>
          <th>informações</th>
          <th>usuário</th>
          <th>visualizado</th>
          <th colspan="3"></th>
        </tr>
        </tr>
        </thead>

        <tbody>


        <% @solicitacao_bloqs.each do |solicitacao_bloq| %>
            <tr>
              <td><%= solicitacao_bloq.cadastro_id %></td>
              <td><%= solicitacao_bloq.informacoes %></td>
              <td><%= solicitacao_bloq.usuario %></td>
              <td><%= solicitacao_bloq.visualizado %></td>
              <td class="col-xs-1">
                <%= link_to solicitacao_bloq do %>
                    <i class="fa fa-2x fa-eye"></i>
                <% end %>
              </td>
              <td class="col-xs-1">
                <%= link_to edit_solicitacao_bloq_path(solicitacao_bloq) do %>
                    <i class="fa fa-2x fa-edit"></i>
                <% end %>
              </td>
              <td class="col-xs-1">
                <%= link_to solicitacao_bloq, method: :delete, data: { confirm: 'Tem certeza que deseja excluir?!' } do %>
                    <i class="fa fa-2x fa-trash"></i>
                <% end %>
              </td>
            </tr>
        <% end %>
        </tbody>
      </table>

      <br>

      <div class="push-right">

      </div>

    </div>
  </div>
</div>